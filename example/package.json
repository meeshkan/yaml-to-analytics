{
  "name": "yarn-to-analytics-example",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf generated && mkdir generated && yaml-to-analytics spec/**/*.yml generated/index.ts"
  },
  "devDependencies": {
    "yaml-to-analytics": "^0.0.4"
  }
}
